# 会话日记 - 2026-04-26

## 会话概述
调查 mempalace mine 进程的高 CPU/内存占用，分析性能瓶颈原因。

## 关键主题

### 1. 性能调查
用户注意到电脑一直在响，怀疑 mempalace 是否持续运行。

**发现的活跃进程：**
- PID 108966: `mempalace mine /home/fengshuai/.claude/projects/-home-fengshuai-projects-python-projects-novel-agent`
- CPU: 70.2%
- 内存: 41.7% (~6.75 GB)
- 运行时间: 8分39秒
- 进程最终完成并终止

### 2. 数据量分析
目标目录：`novel-agent` 项目
- 总大小: 19M
- JSONL 文件: 12个对话转录文件
- 总行数: 9,501条 JSONL 记录
- 估算块数: 约12,000+ drawers（800字符/块，重叠100字符）
- 数据库大小: 872 MB (Qdrant storage.sqlite)

### 3. 根本原因分析

**主要瓶颈：基于CPU的embedding计算**
- 系统未安装torch → `all-MiniLM-L6-v2` 模型在CPU上运行
- CPU embedding比GPU慢10-50倍
- 每个文本块需要计算384维向量

**次要瓶颈：**
- 批处理大小较小: `batch_size=256`（可增至512/1024）
- 同步写入: Qdrant `wait=True` 等待磁盘确认
- 串行处理: 文件逐个处理，无并行
- 无队列机制: `miner.py` 未使用 `queue.py` 异步处理

**性能估算：**
```
CPU embedding每批次（256块）: 约1-2秒
总批次（47批）: 47 × 1.5秒 ≈ 70秒
加上文件读取、实体检测、数据库写入：
总时间: 8-9分钟 ✓ 符合实际观察
```

## 决策

### 优化建议
1. **安装GPU加速（提速10-50倍）：**
   ```bash
   pip install torch
   # 自动检测并使用GPU
   ```

2. **增大批处理大小：**
   - 将 `batch_size=256` 改为 `512` 或 `1024`
   - 文件位置: `mempalace/backends/qdrant.py:61,94`

3. **使用队列异步处理：**
   - 整合 `queue.py` 用于后台操作
   - 不阻塞用户交互

4. **并行文件处理：**
   - 多进程并发挖掘JSONL文件

## 技术细节

### 代码分析
- `miner.py` 使用 `mine_lock()` 保证并发安全，逐文件处理
- `qdrant.py` embedding批处理大小硬编码为256
- `queue.py` 已存在但当前mine工作流未使用
- 实体检测窗口: 每内容扫描5000字符
- 块大小: 800字符，重叠100字符

### 数据库架构
- 后端: Qdrant（默认，WSL2上100%稳定）
- 集合: `mempalace_drawers`, `mempalace_closets`
- 存储: `~/.mempalace/palace/qdrant/collection/`
- 全局模型缓存: `_MODEL_CACHE` 单例模式

## 用户问题解答
**Q: "为什么电脑一直在响，难道mempalace是一直在运行吗"**
A: 是的，mempalace mine进程正在高负载运行（70% CPU，42%内存），处理novel-agent项目。

**Q: "为什么需要这么长时间"**
A: CPU embedding是瓶颈 - 无GPU/torch，batch_size=256保守，同步数据库写入，串行文件处理。12,000+块在CPU上处理8-9分钟是预期的。

## 下一步
进程已成功完成。提供优化方案：
- 安装torch启用GPU加速
- 增大批处理参数
- 整合异步队列处理
- 添加并行文件处理

---
*会话由stop hook保存。进程状态: 已完成（PID 108966已终止）。*